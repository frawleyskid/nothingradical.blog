{{- $pctx := . -}}
{{- if .IsHome -}}
  {{- $pctx = .Site -}}
{{- end -}}

{{- $pages := where $pctx.RegularPages "Section" "posts" -}}

{{- $pages := where $pages "Params.hidden" "!=" true -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}
    {{- $pages = $pages | first $limit -}}
{{- end -}}

{{- $title := .Site.Title -}}
{{- if ne .Title .Site.Title -}}
  {{- with .Title }}{{ $title = printf "%s on %s" . $.Site.Title }}{{ end -}}
{{- end -}}

{{- $xsl := resources.Get "xsl/rss.xsl" | resources.ExecuteAsTemplate "xsl/rss.xsl" . | fingerprint "sha256" -}}

{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\"?>" | safeHTML }}
{{ printf "<?xml-stylesheet type=\"text/xsl\" href=\"%s\" media=\"screen\"?>" $xsl.RelPermalink | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>{{ $title }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent posts {{ if ne .Title .Site.Title }}{{ with .Title }}in {{.}} {{ end }}{{ end }}from {{ .Site.Title }}</description>
    <docs>https://www.rssboard.org/rss-specification</docs>
    <image>
      <url>{{ absURL .Site.Params.rss.image }}</url>
      <title>{{ $title }}</title>
      <link>{{ absURL "/" }}</link>
    </image>
    <generator>Hugo -- gohugo.io</generator>{{ with .Site.LanguageCode }}
    <language>{{.}}</language>{{end}}{{ with .Site.Author.email }}
    <managingEditor>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</managingEditor>{{end}}{{ with .Site.Author.email }}
    <webMaster>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</webMaster>{{end}}{{ with .Site.Copyright }}
    <copyright>{{.}}</copyright>{{end}}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    {{- with .OutputFormats.Get "RSS" }}
    {{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{ end -}}
    {{ range $pages }}
    {{- $content := safeHTML (.Summary | html) -}}
    {{- if .Site.Params.rss.fullContent -}}
    {{- $content = safeHTML (.Content | html) -}}
    {{- end -}}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{ with .Site.Author.email }}<author>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</author>{{end}}
      {{ with .Params.id -}}
      <guid isPermaLink="false">{{ . }}</guid>
      {{ else -}}
      <guid>{{ .Permalink }}</guid>
      {{ end -}}
      <comments>{{ .Permalink }}#comments</comments>
      {{ range .Params.categories -}}
      <category>{{ (printf "/categories/%s" . | $.Site.GetPage).Title }}</category>
      {{ end -}}
      <description>
        {{ $image := partial "helper/image" (dict "Context" . "Type" "rss") -}}
        {{- if $image.exists -}}
        {{ "<" | html }}img src="{{ $image.permalink | absURL }}" alt="Featured image of post {{ .Title }}" {{ "/>" | html}}
        {{- end -}}{{ $content }}
      </description>
      <dc:description>
        {{ safeHTML (.Params.description | html) }}
      </dc:description>
    </item>
    {{ end }}
  </channel>
</rss>